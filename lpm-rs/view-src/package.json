{
  "name": "lpm-ranking-view",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node build.js",
    "check": "tsc --noEmit && node check.js",
    "harness": "node harness/stage.js",
    "harness:rm": "node harness/stage.js --remove"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
