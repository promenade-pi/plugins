[package]
name = "soundness-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Workflow-net structure and soundness diagnosis, independent of any host"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
