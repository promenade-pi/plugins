[package]
name = "metro-map-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Pure Rust conversion from an Object-Centric Petri Net to a metro-map schematic layout graph"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
