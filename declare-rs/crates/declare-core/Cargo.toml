[package]
name = "declare-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "DECLARE constraint templates, discovery and conformance, independent of any host"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
