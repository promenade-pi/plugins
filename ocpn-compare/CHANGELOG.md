# Changelog

## 0.1.1

- Declares `family: "result"` on `OcpnComparison`. The fallback already
  produced the same colour, but the type now states what it is instead of
  relying on the default.

## 0.1.0

Initial release: two-input OCPN comparison with τ-abstracted per-object-type
footprints, structural and variable-arc differences, reciprocal bounded
coverage, and a report viewer.
